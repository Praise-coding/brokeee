import React from 'react';

const LoadingScreenWrapper = ({children}: { children: React.ReactNode }) => {

    return (
        <>
            {children}
        </>
    );
};

export default LoadingScreenWrapper;